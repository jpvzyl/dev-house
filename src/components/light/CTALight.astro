<hr class="section-divider" />

<section class="section cta-section" id="contact">
  <div class="container">
    <div class="cta-layout" data-animate>
      <div class="cta-text">
        <span class="section-eyebrow">Get Started</span>
        <h2 class="cta-heading">
          Ready to build something<br/><em>exceptional?</em>
        </h2>
        <p class="cta-desc">
          Whether you need quantum optimisation, security hardening,
          ISO certification, or a production-grade platform &mdash;
          we have the engineering depth to deliver.
        </p>
      </div>
      <div class="cta-action">
        <a href="mailto:hello@devhouse.dev" class="btn btn-primary btn-lg">
          Start a Conversation
          <svg width="14" height="14" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
        <p class="cta-note">No commitment required. Let's talk about your project.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .cta-section {
    padding-bottom: 3rem;
  }

  .cta-layout {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 4rem;
    align-items: center;
  }

  .cta-heading {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 1rem;
    letter-spacing: -0.01em;
  }

  .cta-heading em {
    font-style: italic;
    color: var(--accent-bright);
  }

  .cta-desc {
    font-size: 0.95rem;
    color: var(--text-secondary);
    line-height: 1.75;
    max-width: 480px;
  }

  .cta-action {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.75rem;
  }

  .btn-lg {
    padding: 0.95rem 2.25rem;
    font-size: 0.95rem;
  }

  .cta-note {
    font-size: 0.78rem;
    color: var(--text-muted);
  }

  @media (max-width: 768px) {
    .cta-layout {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
  }
</style>
